# bob-benchmark 구현 계획

IBM Bob vs Claude Code vs OpenAI Codex vs Cursor 비교 장표 웹페이지.

- 저장소: https://github.com/JOOCHANN/bob-benchmark
- 배포: Cloudflare Pages
- 대상 독자: IBM 직원 (한국어)
- 목적: Bob이 타 AI 코딩 도구와 무엇이 다른지 **한 장으로** 파악 → 궁금한 기능은 **클릭해서 상세 비교**

---

## 1. 확정된 전제

| 항목 | 결정 |
|---|---|
| 기술 스택 | 정적 HTML/CSS/JS (빌드 도구 없음) |
| 표시 언어 | 한국어 |
| 미디어 | 자리(placeholder)만 확보, 실제 이미지/영상은 이후 교체 |
| Bob 정보원 | **공식 제품 문서 최우선** → 사내 발표자료·벤더 발표는 보조. 어긋나면 문서를 따른다 |

빌드 도구를 쓰지 않는 이유: 페이지 종류가 실질적으로 2개(메인 + 상세 템플릿)뿐이라 번들러·SSG의 이득이 없고, Cloudflare Pages에 폴더를 그대로 올리면 끝나기 때문.

---

## 2. 화면 구조

### 2.1 메인 페이지 (`index.html`) — "하나의 장표"

```
┌──────────────────────────────────────────────────────────────┐
│  IBM Bob 기능 비교 장표          [범례: ✅완전 ◐부분 ✕미지원]  │
├──────────────┬────────┬────────┬────────┬────────────────────┤
│              │  Bob   │ Claude │ Codex  │  Cursor            │  ← 상단 도구 헤더(로고+이름)
│              │        │  Code  │        │                    │     스크롤 시 sticky
├──────────────┼────────┼────────┼────────┼────────────────────┤
│ ▸ 개발 경험                                                   │  ← 카테고리 구분 행
│   인터페이스  │   ✅   │   ✅   │   ◐    │   ✅        [상세 →]│  ← 행 전체가 클릭 영역
│   계획 모드   │   ✅   │   ✅   │   ◐    │   ◐         [상세 →]│
│ ▸ 확장성                                                      │
│   MCP 지원    │   ✅   │   ✅   │   ◐    │   ✅        [상세 →]│
│   ...                                                        │
└──────────────────────────────────────────────────────────────┘
```

- 기능명(왼쪽 열)을 누르면 `detail.html?f=<slug>` 로 **페이지 이동**
- 셀은 아이콘 + 짧은 텍스트(예: `✅ 전용 IDE`)를 함께 표기 → 색/기호만으로 판별하지 않아도 됨(접근성)
- 셀 호버 시 한 줄 요약 tooltip
- 항목 수는 23개. 초안의 "한 화면" 목표는 카테고리가 6개로 늘면서 포기했다. 대신 `차이 나는 항목만`
  토글로 동일 판정 항목(현재 5개)을 접는다. 축을 줄여 판단 근거를 빼는 것보다 접는 장치를 두는 편이
  낫다고 봤다. 항목이 많아진 이유는 4장 참조 — 축이 적을 때 그 적음이 Bob에게 유리하게 작용하고 있었다.

### 2.2 상세 페이지 (`detail.html?f=<slug>`)

기능당 페이지 하나. **파일은 1개**이고 쿼리스트링의 slug로 데이터를 골라 렌더한다.
(기능마다 HTML 파일을 복사하면 11개 파일에 같은 레이아웃이 중복되어 유지보수가 무너짐)

**이동은 좌측 목록으로 한다.** 처음에는 이전/다음 방식이었으나 항목이 11개로 늘면서
원하는 항목까지 여러 번 눌러야 해 쓸 수 없었다. 카테고리별로 묶은 목록을 상시 노출해
어느 항목이든 한 번에 이동하도록 바꿨다. 좁은 화면에서는 접어 두고 버튼으로 연다.

레이아웃:

```
━━ 상단 바 (sticky) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 비교 장표로 돌아가기 ━

┌ 좌측 목록 (sticky) ┐  엔터프라이즈 통제
│ SDLC 커버리지      │
│  계획 · 설계        │  # 정책 집행 · 데이터 보호
│  테스트 생성·실행    │  한 줄 요약 + "왜 보는가" 블록
│  배포·파이프라인 연계 │
│  레거시 현대화       │  ┌── Bob ────────────┐ ┌── Claude Code ────┐
│ 엔지니어링 기반     │  │ ● 지원             │ │ ◐ 부분            │
│  ...              │  │ [ 이미지/영상 슬롯 ] │ │ [ 이미지/영상 슬롯 ]│
│ 엔터프라이즈 통제   │  │ • 근거 불릿          │ │ • 근거 불릿        │
│ ▸정책 집행·데이터보호│  │ 출처 ↗              │ │ 출처 ↗            │
│  감사 추적·재현성   │  └───────────────────┘ └──────────────────┘
│ 도입 조건          │
│  ...              │  ## 정리
└──────────────────┘

## 정리
Bob 관점의 차별점 / 한계 (2~4문장, 과장 없이)
```

---

## 3. 데이터 모델 — 단일 소스

`assets/js/data.js` 하나가 **장표와 상세 페이지 양쪽의 유일한 출처**. HTML에 내용을 직접 쓰지 않는다.

```js
const TOOLS = [
  { id: 'bob',    name: 'IBM Bob',    logo: 'assets/img/logo/bob.svg',    highlight: true },
  { id: 'claude', name: 'Claude Code', logo: '...' },
  { id: 'codex',  name: 'OpenAI Codex', logo: '...' },
  { id: 'cursor', name: 'Cursor',      logo: '...' },
];

const FEATURES = [
  {
    slug: 'plan-mode',
    category: '개발 경험',
    name: '계획 모드',
    summary: '구현 전에 작업 계획을 세우고 사용자 승인을 받는 단계',
    tools: {
      bob: {
        level: 'full',              // full | partial | none | unknown
        label: 'Plan 모드 내장',
        bullets: ['...', '...'],
        media: { type: 'image', src: 'assets/img/plan-mode/bob.png',
                 caption: '...', alt: '...' },   // 파일 없으면 placeholder 렌더
        source: { text: 'IBM Bob 공식 문서', url: 'https://bob.ibm.com/docs/ide' },
      },
      claude: { ... }, codex: { ... }, cursor: { ... },
    },
    verdict: 'Bob은 ... / 다만 ...',
  },
];
```

**작성 규칙**

- `level`이 `full`/`partial`/`none`인 셀은 **반드시 `source` 필요**. 근거를 못 찾으면 `unknown`으로
  두고 사용자에게 확인 요청.
- **출처는 주장별로 개별 문서를 건다.** 개요 페이지 하나를 여러 칸에 재사용하면 규칙의 글자만
  지키고 뜻을 어기게 된다. 1차 개정에서 `SRC.claude` 한 개가 열 칸의 근거로 쓰이고 있었다.
- **출처 이름에 근거 등급을 드러낸다.** `공식 문서` > `사내 발표자료`·`벤더 발표` > `보도`.
  `(벤더 발표)`, `(보도)`, `(사내)`를 이름에 붙여 클릭하지 않고도 무게를 알 수 있게 한다.
- **등급을 도구 간에 맞춘다.** 한 도구를 발표자료로 판정하고 다른 도구를 문서 첫 페이지로 판정하면
  판정 차이가 기능 차이가 아니라 조사 밀도 차이가 된다. 이것이 1차 개정의 가장 큰 결함이었다.
- **`none`은 벤더 문서가 부재를 명시한 경우에만 쓴다.** 찾지 못한 것과 없는 것은 다르다. 문서를
  다 훑어도 안 나오면 `unknown`으로 비워 둔다. **비어 있는 칸을 추측으로 채우지 않는다.**
  그리고 부재를 주장하려면 **그 기능이 있을 만한 곳을 모두** 확인해야 한다. 2차 개정에서 IDE
  보안 문서의 "샌드박스가 아니다"라는 한 문장을 근거로 `none`을 적었으나, Bob Shell에 별도
  샌드박스 문서가 있었다. 한 페이지의 부정 서술은 제품 전체의 부재가 아니다.
- **영업 자료와 제품 문서가 어긋나면 제품 문서를 따른다.** 사내 발표자료만이 근거인 주장으로는
  판정을 올리지 않는다. 대신 그 불일치를 칸 안에 적어 검토자가 IBM에 물을 수 있게 한다.
- Bob에 유리하게 임의로 판정하지 않는다. 타 도구가 더 나은 항목은 그대로 표기한다 — IBM 직원용
  내부 분석 자료의 신뢰도가 곧 자료의 가치.
- **기준을 바꾸면 결론이 바뀌는 항목은 그 사실을 `verdict`에 적는다.** 기준에 의존하는 우위를
  절대적 우위처럼 적으면 그것이 편향이다.
- `META.checkedAt` 기록. AI 코딩 도구는 변화가 빨라 자료의 시점 표기가 필수.

---

## 4. 비교 축 분류 체계

**분류 원칙: 카테고리를 기능 묶음이 아니라 "의사결정 질문"으로 나눈다.**

초안에서는 카테고리가 `개발 경험 / 에이전트 / 확장성 / 모델 / 엔터프라이즈` 였는데, 이 방식은
추상화 수준이 뒤섞여 있었다. `개발 경험`은 사용자 경험 층위, `모델`은 운영 층위, `엔터프라이즈`는
조직 층위다. 층위가 섞인 분류는 기술 검토자에게는 자의적으로 보이고, 경영 의사결정자에게는
어느 줄이 자기 판단과 관련 있는지 알려주지 못한다.

각 카테고리가 하나의 질문에 답하도록 다시 묶었다. 같은 표를 보고 기술 검토자와 의사결정자가
각자 필요한 답을 얻는 것이 목적이다.

| 카테고리 | 답하는 질문 | 항목 |
|---|---|---|
| SDLC 커버리지 | 어디까지 자동화되는가 | 계획·설계 / 산출물·문서 체계 / 테스트 생성·실행 / 코드 리뷰·취약점 진단 / 배포·파이프라인 연계 / 레거시 현대화 |
| 엔지니어링 기반 | 실제 개발에서 버티는가 | 코드베이스 이해·작성 / 병렬 에이전트 오케스트레이션 / 사내 시스템 연동 / 확장·커스터마이즈 / 프로그래밍 임베딩 |
| 작업 방식 · 도달 범위 | 언제, 어디서 일을 맡길 수 있는가 | 실행 표면 / 비동기 위임·클라우드 실행 / 예약 실행·무인 자동화 / 팀 협업·비개발자 위임 |
| 엔터프라이즈 통제 | 도입해도 안전한가 | 정책 집행·권한 통제 / 데이터 보호·비밀정보 차단 / 샌드박스·실행 격리 / 감사 추적·재현성 |
| 비용 구조 | 얼마가, 어떻게 드는가 | 과금 구조 / 모델 라우팅·단가 최적화 / 비용 가시성·통제 |
| 도입 조건 | 우리 환경에서 쓸 수 있는가 | 배포 형태·데이터 위치 |

총 23개 항목.

### 4.1 2026-07-29 2차 개정 — 축이 적다는 것이 편향이었다

14개 항목에서 23개로 늘렸다. 늘린 이유는 항목이 부족해서가 아니라, **빠진 축들이 한 방향으로
치우쳐 있었기** 때문이다. 이전 판에서 제외 근거는 이렇게 적혀 있었다.

- `인터페이스 형태, 터미널·셸 통합` → "제품 사양이지 역량이 아니다"
- `Skills / 커스텀 워크플로` → "`사내 시스템 연동`과 판단 근거가 겹친다"

두 근거 모두 틀렸고, 틀린 방향이 우연이 아니었다. 제외된 영역이 정확히 Bob이 가장 약한 지점이었다.

**표면은 사양이 아니라 역량이다.** 표면 수는 "언제 일을 맡길 수 있는가"를 정한다. IDE 안에서만
도는 도구는 개발자가 자리에 앉아 있는 시간에만 쓰인다. 확인 결과 Bob은 Bob-IDE와 Bob Shell 두
표면뿐이고, 나머지 셋은 웹·모바일·Slack에서 작업을 받는다. 이 사실을 빼면 "조직 단위 SDLC
파트너"라는 주장을 검증할 방법이 없어진다.

**확장·커스터마이즈는 MCP와 겹치지 않는다.** MCP는 외부 시스템 연결이고, 규칙·커맨드·Skills·훅은
에이전트 자신의 행동 규정이다. 층위가 다르다. 그리고 이 축을 넣어 보니 Bob은 규칙·슬래시
커맨드·Skills·커스텀 모드를 갖춰 크게 뒤지지 않았다 — 즉 이 축을 뺀 것은 Bob에게 유리한 선택도
아니었다. 단순히 판단 근거를 하나 잃은 것이었다.

새로 세운 카테고리 하나와 항목 아홉 개:

| 신설 항목 | 왜 필요한가 | 결과 (3차 개정 후) |
|---|---|---|
| 산출물·문서 체계 | 단계별 산출물을 제품이 정의하는지 | **네 도구 동일** — 발표자료 주장이 문서에 없었다 |
| 확장·커스터마이즈 | 규칙을 팀이 공유하고 훅으로 강제할 수 있는지 | Bob 부분 (훅 없음) |
| 프로그래밍 임베딩 | 에이전트를 사내 도구에 부품으로 넣을 수 있는지 | Bob 부분 (SDK 없음) |
| 실행 표면 | 개발자 책상을 벗어난 표면이 있는지 | Bob 열세 |
| 비동기 위임·클라우드 실행 | 로컬을 벗어난 곳에서 작업이 도는지 | Bob 확인 필요 |
| 예약 실행·무인 자동화 | 사람이 없는 시간에도 도는지 | Bob 확인 필요 |
| 팀 협업·비개발자 위임 | 코드를 쓰지 않는 구성원이 쓸 수 있는지 | Bob 확인 필요 |
| 데이터 보호·비밀정보 차단 | (기존 항목을 분리) 유출을 제품이 막는지 | 네 도구 모두 부분 |
| 샌드박스·실행 격리 | OS·컨테이너 수준 격리가 있는지 | **Bob 부분** — Shell에 샌드박스가 있었다 |

`정책 집행·데이터 보호`를 셋으로 쪼갠 이유는 한 칸에 서로 다른 판정이 섞여 있었기 때문이다.
쪼개고 나서 각각을 공식 문서로 확인하니 세 결과가 모두 달랐다 — 권한 통제는 좌석·예산에만 미치고,
실행 격리는 Shell에만 있고, 데이터 보호는 문서가 스스로 쓰기 우회 가능성을 밝힌다. 이 셋을 한 칸에
묶어 `지원`으로 적으면 가장 약한 부분이 가장 강한 부분에 가려진다.

### 4.2 근거 등급을 판정과 분리한다

1차 개정의 규칙(`full`/`partial` 셀에는 `source` 필수)은 형식적으로만 지켜지고 있었다. Bob의
`지원`은 보도자료와 개인 블로그에 기대고, 경쟁 도구의 `부분`은 문서 첫 페이지 하나를 열 개
주장의 출처로 재사용했다. 같은 규칙을 통과하지만 근거의 무게가 전혀 달랐고, 그 차이가 그대로
판정 차이로 나타났다.

그래서 출처 이름에 등급을 드러낸다. **`공식 문서` > `사내 발표자료`·`벤더 발표` > `보도`.**
`SRC` 항목 이름 자체에 `(벤더 발표)`, `(보도)`, `(사내)`를 붙여, 표를 읽는 사람이 근거의 무게를
클릭하지 않고도 알 수 있게 했다. 그리고 각 항목의 주장을 뒷받침하는 개별 문서를 따로 걸었다
— 개요 페이지 하나를 모든 칸에 재사용하지 않는다.

이 기준을 적용하자 네 칸의 판정이 바뀌었다.

- `배포·파이프라인 연계` — Bob `지원`→`부분`. Bob 근거는 "조율한다"는 발표 문구이고, 나머지
  셋은 벤더가 관리하는 CI 통합 경로를 문서로 갖고 있다.
- `정책 집행·권한 통제` — 경쟁 도구 `부분`→`지원`. Claude의 MDM 관리 설정과 Codex의 RBAC이
  같은 요건을 충족한다. 이전 판의 차이는 기능 차이가 아니라 근거 등급 차이였다.
- `감사 추적·재현성` — Bob `지원`→`부분`, Codex `부분`→`지원`.
- `비용 가시성·통제` — Claude `부분`→`지원`. 분석 API 두 종과 지출 알림이 있다.

### 4.3 2026-07-29 3차 개정 — 발표자료보다 공식 문서를 우선한다

2차 개정은 Bob 판정 7개를 사내 발표자료에 기대고 있었다. 발표자료는 영업 자료이므로 제품이
실제로 하는 일과 어긋날 수 있다. 그래서 `bob.ibm.com` 사이트맵에서 docs 페이지를 전수 확보해
52개를 읽고 모든 Bob 칸을 다시 세웠다. 결과적으로 Bob의 1차 문서 근거가 12칸에서 19칸으로 늘었다.

**발표자료에는 있고 공식 문서에는 없던 것.** 이 방향의 불일치가 더 많았다.

| 주장 | 공식 문서 확인 결과 | 조치 |
|---|---|---|
| SDLC 단계별 산출물, Phase gate, 의사결정 기록서 | SRS·SDD·deliverable·phase gate·추적성이 **한 건도** 없음 | `산출물·문서 체계` Bob `지원`→`부분`. 네 도구 동일이 됐다 |
| 조직 차원의 Task 권한 정책 | 역할은 User·Admin 둘뿐. 규칙은 "공유 저장소를 각자 clone"으로 배포하는 관례이고, 자동 승인은 개인이 켜는 툴바 설정 | `정책 집행·권한 통제` Bob `지원`→`부분` |
| 민감 데이터 자동 스캔 | 문서화되지 않음. 오히려 `.bobignore`의 **쓰기 우회 가능성**을 문서가 직접 밝힘 | `데이터 보호` 부분 유지, 근거를 공식 문서로 교체 |
| 멀티모델 라우팅 (Claude·Mistral·Granite) | 모델·라우팅·모델 선택 UI가 문서 전체에 **없음**. Bobcoins 문서만 "모델 사용량을 코인으로 환산"이라고 적음 | `모델 라우팅` `지원` 유지(발표·보도 근거), 단 문서 부재를 칸에 명시 |
| 라우팅 절감 폭 | 문서 없음. 발표자료 수치는 최대 40% | 2차 개정에서 이미 정정 |

**공식 문서에만 있고 발표자료에는 없던 것.** 반대 방향도 있었으므로 한쪽만 의심하면 안 된다.

| 문서에서 발견 | 조치 |
|---|---|
| Bob Shell의 샌드박스 — macOS Seatbelt(프로파일별 네트워크·쓰기 제어)와 Docker·Podman 완전 프로세스 격리. 플래그·환경변수·`settings.json`으로 설정 | `샌드박스` Bob `미지원`→`부분`. **2차 개정의 오판을 정정** |
| 신뢰 폴더 — 미승인 폴더를 제한 모드로 실행해 프로젝트 설정·환경변수·MCP·자동 승인·커스텀 커맨드를 모두 차단 | `데이터 보호`에 반영 |
| Enterprise 활동 로그 — CADF 표준 JSON, `action`·`outcome`·`eventTime`·`initiator.id`, 시간 단위 파일 | `감사 추적`에 반영. 다만 기록 대상이 인증·관리 이벤트뿐이어서 `부분` 유지 |
| 팀 예산과 개인 지출 한도, Bobalytics 3층 구조와 KPI 3종(도입률·Bob factor·Bobcoin 지출) | `비용 가시성` `지원` 유지, 근거가 발표자료에서 공식 문서로 승격 |
| Java 현대화 4개 워크플로(업그레이드·Liberty 리플랫포밍·UI 현대화·단위 테스트 생성) | `레거시 현대화` `지원` 유지, 근거 승격 |
| 코드 리뷰가 "전적으로 IDE 안에서" 돈다는 명시. GitHub·GitLab 브랜치 비교, 이슈 검증은 GitHub 전용 | `코드 리뷰`의 기준 의존성 서술이 추정에서 확인된 사실로 바뀜 |
| 비대화형 세션은 IBMid가 아니라 **API 키 인증** 필요 | `배포·파이프라인 연계`에 운영 조건으로 추가 |
| Bob IDE가 `.pkg`/`.exe`/`.deb`/`.rpm`으로 설치되는 독립 애플리케이션 | `실행 표면` 서술 정정 |

**문서 전수 검색으로 부재를 확인한 것.** 훅, cron·예약 실행, 클라우드·백그라운드 에이전트,
Slack·모바일·JetBrains, SDK는 52개 페이지에서 **한 건도** 나오지 않았다. 그래도 `미지원`으로
올리지 않고 `확인 필요`로 둔다 — 문서 부재는 기능 부재의 증거가 아니다. 다만 훅과 SDK는 대체
수단(규칙·Skills, Bob Shell 비대화형)이 있어 해당 항목을 `부분`으로 뒀다.

**이 개정이 남긴 교훈.** 한 자료만 보면 양방향으로 틀린다. 발표자료만 보면 없는 기능을 있다고
적고, IDE 문서만 보면 있는 기능을 없다고 적는다(샌드박스가 그랬다). 판정 근거는 제품 문서를
우선하되, 문서에 없다는 사실을 곧바로 부재로 옮기지 않는 것이 이 자료의 규칙이다.

`과금 구조`는 판정은 그대로 두고 수치를 고쳤다. Pro Plus 티어 누락, 좌석마다 붙는 지원 비용
($3/$9/$30), 월 할당량 이월 불가, 엔터프라이즈 팩(1,000코인 $500·1년 만료), overage를 한 번 켜면
그 달에 끌 수 없다는 조건이 빠져 있었다. 출처도 개인 블로그에서 공식 Bobcoins 문서로 옮겼다.

`모델 라우팅`의 절감 폭은 "3분의 1~절반"으로 적혀 있었는데, IBM 발표자료가 제시하는 값은
**최대 40%**다. 우리 자료가 벤더 주장보다 후하게 적고 있었다.

### 4.4 유지되는 설계 원칙

**비용 구조를 카테고리로 세운 이유 (1차 개정).** 초안에서는 가격을 통째로 뺐다. "계약 조건에
따라 달라져 공개 정가로 비교하면 오해를 만든다"는 것이 이유였는데, 이 논리는 네 도구에 똑같이
적용되므로 상쇄된다. 남는 것은 "비용을 빼면 경영 의사결정자가 쓸 수 없는 자료가 된다"는 사실뿐이다.
대신 원래 우려는 판정 기준으로 흡수했다 — **금액의 높낮이가 아니라 비용 구조를 비교한다.**
표에 적힌 금액은 모두 공개 정가임을 항목 안에 명시한다.

**판정 기준을 항목 안에 적는다.** `지원 / 부분 / 미지원`의 의미가 항목마다 달라질 위험이 있으므로,
기준이 자명하지 않은 항목은 `why`에 무엇을 기준으로 판정했는지 한 문장으로 못 박는다. 기준이
없으면 판정은 자의적으로 보인다. 2차 개정에서는 여기에 한 가지를 더했다 — **기준을 바꾸면 결론이
바뀌는 항목은 그 사실을 `verdict`에 적는다.** 예를 들어 `코드 리뷰`는 "목록으로 남는가"로 물으면
Bob이 앞서지만 "사람이 잊어도 리뷰가 도는가"로 물으면 뒤진다. 기준에 의존하는 우위를 절대적
우위처럼 적으면 그것이 편향이다.

**`미지원`과 `확인 필요`를 구분한다.** `미지원`은 벤더 문서가 부재를 명시한 경우에만 쓴다.
3차 개정 시점에 해당하는 칸은 **없다**. 2차 개정에서 `샌드박스·실행 격리`의 Bob을 `미지원`으로
적었는데, 근거로 삼은 문장(".bobignore는 시스템 수준 샌드박스를 만들지 않는다")은 IDE 문서였고
Bob Shell에는 별도의 샌드박스 문서가 있었다. **한 페이지의 부정 서술을 제품 전체의 부재로 읽은
것이 오판의 원인이다.** 부재를 주장하려면 그 기능이 있을 만한 곳을 모두 확인해야 한다.

예약 실행·비동기 위임·비개발자 표면은 docs 52개 페이지와 발표자료를 모두 훑어도 찾지 못했지만
`확인 필요`로 비워 둔다. **비어 있는 칸을 추측으로 채우지 않는다.**

**설계상 의도한 두 가지**

1. **네 도구가 모두 동일한 항목을 숨기지 않는다.** 코드베이스 이해, 테스트, 사내 시스템 연동,
   데이터 보호는 현재 네 도구가 같은 수준이며 장표에 `동일`로 드러낸다. 이 항목들을 빼면 표는
   Bob이 일방적으로 앞서는 그림이 되지만, 검토자가 직접 확인하는 순간 자료 전체의 신뢰가 무너진다.
   다만 `동일`의 설명 문구는 고쳤다 — "도구 선택의 근거가 되지 않는다"는 표현은 Bob이 앞서지
   않는 행을 할인하도록 유도한다. "도구를 가르지 못한다는 뜻이며 중요하지 않다는 뜻은 아니다"로 바꿨다.
2. **Bob이 뒤처지는 항목을 반드시 포함한다.** 1차 개정에서는 `배포 형태·데이터 위치` 한 줄이
   전부였고, 그 결과 Bob은 14개 항목 중 `지원` 11 / `부분` 3 / `미지원` 0이었다. 축을 바로잡고
   근거를 공식 문서로 세운 현재 분포는 `지원` 9 / `부분` 11 / `미지원` 0 / `확인 필요` 3이고,
   Bob이 뒤지는 행이 12개, 단독 우위인 행이 2개(`코드 리뷰`, `레거시 현대화`)다.
   **후자가 이 자료의 신뢰도를 만드는 숫자다.**

### 4.5 개정으로 드러난 실제 결론

축을 바로잡고 나서 자료의 결론이 바뀌었다. 1차 개정의 결론은 "기본기는 평준화됐으니 판단 축을
통제 항목으로 옮겨야 한다"였는데, 통제 항목을 세분하자 그곳에서도 Bob이 일방적이지 않았다.

현재 자료가 지지하는 결론은 세 문장이다. 3차 개정에서 2번이 좁아졌다.

1. 코딩 기본기로는 네 도구를 가를 수 없다 (변함없음).
2. Bob의 값은 **레거시 현대화 전용 워크플로**와 **비용 거버넌스**(행위 단위 원가 + 팀 예산·개인
   한도), 그리고 **Findings 목록**이다. 셋 다 공식 문서로 확인된다. 2차 개정이 여기에 넣었던
   `SDLC 산출물 체계`는 문서 근거가 없어 빠졌다.
3. Bob의 제약은 **개발자 책상 위에서만 돈다**는 것이다. 표면 2개, 예약 실행·비동기 위임 없음,
   에이전트 행위 정책과 행위 감사 로그 없음, 격리는 Shell 전용, SaaS 전용.

그리고 판단을 단순하게 만드는 사실이 하나 있다. IBM은 Anthropic과 파트너십을 맺고 Claude를
Bob의 프런티어 모델로 쓴다. 따라서 이 비교는 상당 부분 **"Claude에 IBM의 거버넌스와 현대화
자산을 얹은 것"과 "Claude 원본"의 비교**다. 모델 품질은 변수가 아니므로, 얹은 것의 값이 표면·
자동화 제약의 값을 넘는지만 판단하면 된다. 라우팅이 절감하는 40%도 이 맥락에서 읽어야 한다 —
아끼는 대상이 Claude 호출이고, 아낀 만큼 결과물이 소형 모델 산출물로 바뀐다.

---

## 5. 파일 구조

배포되는 파일은 `public/` 안에만 둔다. 문서 파일이 배포에 섞여 들어가지 않게 하기 위함이며,
Cloudflare 설정에서도 이 폴더 하나만 가리키면 된다.

```
bob-benchmark/
├── public/                   # ← 배포 대상은 이 폴더뿐
│   ├── index.html            # 장표
│   ├── detail.html           # 상세 템플릿 (?f=slug)
│   └── assets/
│       ├── css/style.css     # 전체 스타일 (파일 1개)
│       ├── js/
│       │   ├── data.js       # 단일 데이터 소스
│       │   ├── table.js      # 장표 렌더
│       │   └── detail.js     # 상세 렌더
│       └── img/
│           ├── logo/         # bob.svg, claude.svg, codex.svg, cursor.svg
│           └── <slug>/       # plan-design/bob.png, surfaces/cursor.mp4 ...
├── internal-src/             # ← 사내 원본 자료. .gitignore로 제외. 배포에도 안 들어간다
├── wrangler.jsonc            # Cloudflare 배포 설정 (public/만 업로드)
├── PLAN.md
├── README.md                 # 자료 갱신 방법 (data.js 편집법, 이미지 추가법)
└── .gitignore
```

`internal-src/`는 IBM 발표자료 같은 사내 원본을 두는 곳이다. 공개 저장소에 올라가면 안 되므로
`.gitignore`에 넣었고, 배포 대상이 `public/` 하나뿐이라 Cloudflare 쪽으로도 새지 않는다.
자료를 여기서 인용할 때는 `SRC`에 `url: null`인 항목으로 넣는다 — 링크 없는 출처는 상세 페이지가
이름만 표시한다.

**미디어 파일명 규칙:** `assets/img/<feature-slug>/<tool-id>.<png|mp4>`
파일이 없으면 상세 페이지에 "이미지 준비 중" 점선 박스가 자동 표시되고, 나중에 규칙대로 파일만 넣으면 반영된다.

---

## 6. 디자인 방향

- IBM Carbon 느낌의 절제된 톤 (IBM Plex Sans 웹폰트 또는 시스템 폰트 폴백)
- Bob 열은 미묘한 배경 강조 — 눈에 띄되 "광고"처럼 보이지 않을 정도로
- 라이트/다크 모드 대응 (`prefers-color-scheme`)
- 반응형: 데스크톱 = 표 그대로 / 태블릿 이하 = 표 가로 스크롤(`overflow-x:auto`), 본문은 가로 스크롤 금지
- 외부 CDN 의존 없음 → 사내망·오프라인에서도 동작

---

## 7. 구현 단계 및 검증

**현재 진행 상황 (2026-07-29, 2차 개정 후):** 1·3·4·5단계 완료. 4장의 분류 체계로 23개 항목을
채웠고 장표·상세 페이지 모두 동작한다. 남은 일은 **2단계(92개 판정 칸 검증)** 와 **6단계(배포)** 다.

2단계가 이 자료의 성패를 가른다. 2차 개정에서 근거 등급을 분리하면서 검증 상태가 칸마다 눈에
보이게 됐고, 그 결과 우선순위가 다시 정해졌다.

**A. Bob에게 물어야 하는 것 (현재 `확인 필요`로 비워 둔 칸)**

공식 문서·체인지로그(v1.0.1~v2.0.1)·사내 발표자료를 모두 훑어도 찾지 못한 기능들이다. 없다고
확정할 근거가 없어 비워 두었으므로, IBM 담당자에게 직접 확인해야 한다.

1. **예약 실행 / cron / 이벤트 트리거** — 경쟁 도구 셋 모두 제공한다. Bob의 강점인 보안 스캔과
   품질 점검이 반복 작업이라 이 격차가 실제 운영에서 누적된다.
2. **비동기 클라우드 에이전트** — 대규모 현대화에서 로컬 IDE 한 대가 병목이 된다.
3. **비개발자용 표면** — 발표자료가 13개 직무를 대상으로 그리는데 확인된 접근 경로는 IDE와
   터미널뿐이다. 이 간격을 해소하지 못하면 "조직 단위 SDLC 파트너" 주장을 자료에 실을 수 없다.
4. **훅(hooks)** — 규칙을 코드로 강제할 수 있는지. "조직 표준 강제"가 도입 명분이면 핵심에 걸린다.
5. **에이전트 SDK / 헤드리스 임베딩** — 사내 플랫폼에 넣을 계획이 있을 때만 중요하다.
6. **감사 로그의 실제 형식** — 보존 기간, 반출 형식, 변조 방지. 컴플라이언스 요건과 직접 맞춰야 한다.
7. **웹·모바일·Slack 연동 로드맵** — 현재 없다면 시점.

**B. 파일럿에서 직접 뚫어 봐야 하는 것**

- **데이터 보호·비밀정보 차단** — 이 자료에서 주장과 문서가 가장 크게 어긋난 칸이다. IBM 발표는
  민감 데이터 스캔과 실시간 정책 집행을 말하는데, 공식 보안 가이드는 이를 자동 기능으로 문서화하지
  않고 사용자 책임으로 안내한다. 저장소에 테스트 비밀값을 심어 두고 스캔이 실제로 걸리는지,
  승인 장치가 프롬프트 인젝션으로 우회되는지 확인한다. 프리뷰 단계에 CLI를 통해 악성코드가 실행된
  사례가 보고된 적이 있고(The Register, 2026-01-07) IBM은 조치를 밝혔으나 공개 검증 자료는 없다.
- **샌드박스 부재의 실제 위험** — 자동 승인 목록을 켠 상태에서 위험 명령이 어디까지 통과하는지.
- **Findings의 오탐률** — 형태(목록으로 남는다)의 우위는 확인됐으나 진단 품질은 별개다.
- **Bobalytics의 배부 적합성** — 대시보드가 있다는 것과 우리 부서별 정산 기준에 맞는다는 것은 다르다.
- **Bob Shell의 CI 동작** — `배포·파이프라인 연계`를 `부분`으로 내린 근거가 전용 CI 통합의 부재다.
  우리 파이프라인에서 실제로 돌려 보고 확정한다.

**C. 수치로 다시 재야 하는 것**

- **비용 구조 3개 항목 전부** — 요금제는 가장 빨리 바뀌고 가장 자주 인용된다. 표의 금액은 공개
  정가이므로 우리 계약 조건과 예상 작업량으로 다시 계산한다. 특히 코인 이월 불가와 좌석당 지원
  비용이 총소유비용에 미치는 영향.
- **모델 라우팅 40% 절감** — IBM 발표자료 수치이며 작업 구성에 따라 달라진다. 우리 워크로드를
  얼마간 흘려 보고 실제 라우팅 분포로 다시 잰다. 절감과 함께 품질 영향도 같이 재야 한다.
- **현대화 사례 수치** — Blue Pearl(30일→3일), APIS IT(.NET 4~5시간), 테스트 커버리지 0→92%는
  모두 IBM이 고른 성공 사례이고 대상 코드베이스 상태에 크게 좌우된다. 우리 자산으로 소규모
  파일럿을 돌려 배율을 직접 얻는다.

**D. 경쟁 도구 쪽에 남은 미확인 칸**

공정성을 위해 경쟁 도구의 미확인 항목도 적어 둔다. 한쪽만 엄격하게 검증하면 그것도 편향이다.

- Codex의 비용 가시성 — 워크스페이스 분석의 실제 분해 범위와 사전 한도 설정 여부
- Cursor의 사전 한도 설정, 로컬 에이전트의 OS 수준 격리 여부
- Cursor의 한 작업 내 서브에이전트 분기 범위
- Claude·Codex·Cursor의 제품 내장 민감정보 스캐너 유무
- Claude Routines가 리서치 프리뷰라는 점 — GA 시점과 한도 변경 가능성

```
1. 기능 축 확정 (15 → 11 → 14 → 23개, 카테고리 6개 확정)
   → 검증: 사용자 승인. 각 축이 "도구 간 차이가 실제로 존재하는가" 통과
   → 2차 개정 추가 검증: 제외한 축이 한 도구에 유리한 방향으로 치우치지 않았는가

2. 4개 도구 × 23개 항목 = 92셀 조사, data.js 작성
   → 검증: full/partial/none 셀 전부에 source 존재. 없으면 unknown 표기
   → 검증: 출처가 개요 페이지 재사용이 아니라 주장별 개별 문서인가
   → 검증: 근거 등급이 도구 간에 비대칭이 아닌가 (Bob만 발표자료로 판정하지 않았는가)

3. index.html + table.js + style.css — 장표 렌더
   → 검증: 기능 행 23개, 카테고리 행 6개, 판정 셀 92개 렌더
   → 검증: 기능명 클릭 시 detail.html?f=slug 로 이동, 필터가 동일 판정 4행을 접음

4. detail.html + detail.js — 상세 페이지 렌더
   → 검증: 23개 slug 모두 정상 렌더(카드 4개·배지 4개·정리 존재),
           잘못된 slug는 안내 문구 + 목록, 미디어·출처 URL이 없어도 레이아웃 유지

5. 반응형·다크모드·접근성 점검
   → 검증: 375/768/1440px에서 본문 가로 스크롤 없음, 표 헤더 sticky 동작,
           키보드 Tab으로 모든 기능 행 접근 및 Enter로 이동 가능

6. GitHub push + Cloudflare Pages 연결
   → 검증: 배포 URL에서 장표 → 상세 → 뒤로가기 왕복 정상 동작
   → 검증: internal-src/ 가 저장소에 올라가지 않았는가
```

3·4단계는 jsdom으로 자동 검사했다. 스크립트는 저장소에 넣지 않았으므로 축을 늘릴 때 다시
확인하려면 같은 검사를 재작성해야 한다 — 항목 수·카테고리 수·판정 셀 수, 근거 없는 판정,
카테고리가 표에서 분산되는지, 23개 slug의 렌더 오류를 본다.

---

## 8. 배포 (Cloudflare)

빌드 없는 정적 사이트이므로 설정이 단순하다. 두 경로 모두 열어둔다.

**Git 연동 (권장)**

1. Cloudflare Dashboard → Workers & Pages → Create → Pages → Connect to Git
2. `JOOCHANN/bob-benchmark` 선택
3. 설정: **Framework preset = None / Build command = 비움 / Build output directory = `public`**
4. `main` 브랜치 push마다 자동 재배포

**wrangler**

`npx wrangler deploy` — `wrangler.jsonc`의 `assets.directory`가 `./public`을 가리킨다.
Worker 스크립트는 필요 없다. 정적 자산만으로 배포된다.

주의할 점 두 가지:

- 상세 페이지가 쿼리스트링(`?f=slug`) 방식이므로 SPA 라우팅 설정이나 `_redirects`가 필요 없다.
- 대시보드 업로더의 "This uploader currently only supports static assets" 문구는 오류가 아니라
  상시 표시되는 안내다. 이 프로젝트는 HTML/CSS/JS만 쓰므로 해당 업로더의 지원 범위 안에 있다.
  동적 애플리케이션으로 전환할 이유가 되지 않는다.

---

## 9. 확인이 필요한 사항

사용자 확답이 필요한 것:

1. **비교 대상 확정** — "Codex"는 OpenAI Codex CLI/클라우드 에이전트를 말하는 게 맞는지?
   (같은 이름의 구 모델과 구분 필요)
2. **Bob 버전 기준** — 사내에서 쓰는 Bob이 공개 GA 버전과 동일한지, 사내 전용 기능이 따로 있는지.
   7장 A의 7개 항목이 여기에 걸린다. 사내 빌드에 예약 실행이나 웹 표면이 있다면 표가 달라진다.
3. **공개 여부** — 이 페이지가 사내 전용인지 공개용인지. 공개용이면 IBM 로고·상표 사용과 경쟁사
   비교 표현에 대한 검토가 필요하다. 특히 2차 개정으로 Bob이 뒤지는 행이 11개가 됐으므로,
   사내 자료로서는 정확하지만 공개 자료로서는 사내 승인 절차가 필요할 수 있다.
4. **사내 발표자료 인용 범위** — 현재 `산출물·문서 체계`, `정책 집행·권한 통제`, `비용 가시성`,
   `모델 라우팅` 네 항목이 사내 발표자료를 근거로 한다. 발표자료 원본은 `internal-src/`에 두고
   저장소에서 제외했으나, 자료의 내용을 웹페이지에 인용하는 것이 사내 기준에 맞는지 확인이 필요하다.
   맞지 않으면 해당 칸들을 공개 문서 근거로 다시 찾거나 `확인 필요`로 내려야 한다.
5. **경쟁사 요금 정보 인용** — `비용 구조` 3개 항목에 경쟁사 공개 정가를 인용하고 있다.
   사내 문서에 인용하는 데 대한 기준 확인.

1은 답이 없어도 작업을 진행할 수 있다. 2는 표의 `확인 필요` 3칸을 확정하는 데 필요하다.
3~5는 배포 전에 정해야 한다.

## 조사 출처 (2026-07-29 기준)

근거 등급을 함께 적는다. 판정에 쓸 때 이 등급이 도구 간에 비대칭이 되지 않게 한다.

**Bob — 1차 제품 문서 (판정의 기본 근거)**

`bob.ibm.com/sitemap.xml`에서 docs 페이지 목록을 뽑아 52개를 전수 확인했다. 아래는 판정에 직접
쓴 것만 추린 목록이다.

- [IDE 문서](https://bob.ibm.com/docs/ide) · [Bob Shell 문서](https://bob.ibm.com/docs/shell)
- [체인지로그](https://bob.ibm.com/docs/ide/changelog) — v1.0.1(2026-03)~v2.0.1(2026-07).
  기능 도입 시점 확인용. 서브에이전트·코드 리뷰가 v2.0.0(2026-06), Bobalytics for Enterprise가
  v1.0.2(2026-04), 컨텍스트 200K→270K가 v2.0.0
- [설치](https://bob.ibm.com/docs/ide/getting-started/install) — `.pkg`/`.exe`/`.deb`/`.rpm`
  독립 애플리케이션. 실행 표면 판정의 근거
- [모드](https://bob.ibm.com/docs/ide/features/modes) — Agent·Ask·Plan 3종, 모드별 도구 접근,
  Plan은 create-plan 스킬 사용, Plan→Agent 자동 전환
- [서브에이전트](https://bob.ibm.com/docs/ide/features/subagents) — 독립 컨텍스트, 생성 전 승인
- [코드 리뷰](https://bob.ibm.com/docs/ide/features/code-reviews) — **"전적으로 IDE 안에서 돈다"**
  는 명시, GitHub·GitLab 브랜치 비교, 이슈 검증은 GitHub 전용
- [Skills](https://bob.ibm.com/docs/ide/features/skills) ·
  [슬래시 커맨드](https://bob.ibm.com/docs/ide/features/slash-commands) ·
  [커스텀 규칙](https://bob.ibm.com/docs/ide/configuration/rules) — 확장 수단. 규칙 배포는
  "공유 저장소를 팀원이 각자 clone" 하는 관례
- [도구](https://bob.ibm.com/docs/ide/core-concepts/tools) — Read·Edit·Execute·MCP·Skill·Todo·Subtask
- [Bobalytics](https://bob.ibm.com/docs/ide/features/bobalytics) — 워크스페이스·팀·사용자 3층,
  KPI 3종(도입률·Bob factor·Bobcoin 지출), Enterprise 전용
- [엔터프라이즈 개요](https://bob.ibm.com/docs/ide/enterprise/enterprise-index) ·
  [팀·예산](https://bob.ibm.com/docs/ide/enterprise/getting-started/teams) ·
  [사용자·역할](https://bob.ibm.com/docs/ide/enterprise/getting-started/users) —
  공유 코인 풀, 팀 예산 상한, 개인 지출 한도, 역할은 User·Admin 둘뿐
- [SAML IdP](https://bob.ibm.com/docs/ide/enterprise/getting-started/identity-providers) —
  도메인별 IdP, DNS TXT 소유 검증 후 SSO 강제
- [활동 로그](https://bob.ibm.com/docs/ide/enterprise/getting-started/activity-log) — **CADF 표준**
  JSON, `action`·`outcome`·`eventTime`·`initiator.id`. 단 기록 대상은 인증·관리 이벤트뿐
- [보안 가이드](https://bob.ibm.com/docs/ide/security/bob-security-guidance) ·
  [.bobignore](https://bob.ibm.com/docs/ide/configuration/bobignore) — 읽기는 엄격 차단이나
  **편집 도구의 쓰기 우회 가능성**을 문서가 직접 밝힘
- [Bob Shell 샌드박스](https://bob.ibm.com/docs/shell/security/sandboxing) — macOS Seatbelt
  프로파일(네트워크·쓰기 제어)과 Docker·Podman 완전 프로세스 격리. 플래그·환경변수·`settings.json`
- [신뢰 폴더](https://bob.ibm.com/docs/shell/security/trusted-folders) — 미승인 폴더는 제한 모드
- [비대화형 세션](https://bob.ibm.com/docs/shell/getting-started/start-bobshell-non-interactive) —
  `bob -p`, 파이프, **API 키 인증 필요**(IBMid 불가)
- [Bobcoins](https://bob.ibm.com/docs/ide/account/bobcoins) — 코인 단가·플랜별 할당·이월 불가·
  overage 조건·엔터프라이즈 팩
- [Java 현대화 워크플로](https://bob.ibm.com/docs/ide/premium-packages/java-modernization/workflows)
  — 업그레이드·Liberty 리플랫포밍·UI 현대화·단위 테스트 생성 4종

**전수 검색으로 부재를 확인한 것 (52개 페이지에서 0건)**

훅, cron·예약 실행, 클라우드·백그라운드 에이전트, Slack·모바일·JetBrains, SDK, 그리고
**모델 라우팅·Granite·Mistral·모델 선택 UI**. 모델 관련 언급은 Bobcoins 문서의 "Bob이 모델
사용량을 코인으로 환산한다" 한 줄뿐이다. 부재를 `미지원`으로 옮기지 않고 `확인 필요`로 둔다.

**Bob — 사내 발표자료 (링크 없음, `internal-src/`)**

`IBM Bob 기획부터 배포까지 바이브코딩하는 AI-powered SDLC 파트너 발표자료.pdf` (33쪽).
**공식 문서와 교차 확인한 결과를 함께 적는다.** 판정은 문서를 따랐다.

| 발표자료 내용 | 공식 문서 확인 |
|---|---|
| SDLC 단계별 산출물, Phase gate, 의사결정 기록서 (7·8쪽) | ✗ 없음 — 판정 근거로 쓰지 않음 |
| 조직·팀·개인 사용 한도 (11쪽) | ✓ 확인 — 팀 예산·개인 지출 한도로 문서화 |
| Task 권한 정책의 조직 차원 통제 (11쪽) | ✗ 없음 — 역할은 User·Admin 둘뿐 |
| 모델 목록: Claude / Mistral·Devstral / Granite (13쪽) | ✗ 문서에 모델 언급 없음 (벤더 발표·보도로만 확인) |
| 라우팅 절감 최대 40% (23쪽) | ✗ 문서 없음. 이전 판의 "3분의 1~절반"은 이 값으로 정정 |
| Rules·Skills·AGENTS.md (30쪽) | ✓ Rules·Skills 확인 (AGENTS.md는 미확인) |
| Bob Findings의 SQL Injection·XSS 탐지 (23쪽) | ✓ 코드 리뷰 문서로 확인 |
| Anthropic 기술 파트너십 (3쪽) | 문서 범위 밖. Bob의 프런티어 모델이 Claude라는 근거 |
| 고객 사례: Blue Pearl 30일→3일, APIS IT .NET 4~5시간, Novacomp 2일 | 문서 범위 밖. IBM 제시 사례로 표기 |
| IBM 컨설턴트 2,800명·만족도 4.5/5·생산성 77% (25쪽) | 내부 조사 수치로 표기 |

**Bob — 벤더 발표·보도 (판정 근거로는 가장 낮은 등급)**

- [출시 발표 (2026-04-28)](https://newsroom.ibm.com/2026-04-28-introducing-ibm-bob-ai-development-partner-that-takes-enterprises-from-ai-assisted-coding-to-production-ready-software) — GA, SaaS 전용, 온프렘은 향후 릴리스
- [멀티에이전트·비용 분석 발표 (2026-07-09)](https://newsroom.ibm.com/2026-07-09-ibm-advances-enterprise-ai-software-development-with-multi-agent-capabilities-and-specialized-modernization-workflows)
- [Premium Package for Java](https://www.ibm.com/new/announcements/announcing-ibm-bob-premium-package-for-java-modernization)
- [GA 보도 (The Register)](https://www.theregister.com/software/2026/04/28/ibms-ai-coding-partner-bob-hits-general-availability/5226774) — 모델 라우팅의 블랙박스 지적, 보안 취약점 언급
- [멀티모델 라우팅 (VentureBeat)](https://venturebeat.com/orchestration/ibm-launches-bob-with-multi-model-routing-and-human-checkpoints-to-turn-ai-coding-into-a-secure-production-system)
- [요금제 상세 (IT Jungle, 2026-06-01)](https://www.itjungle.com/2026/06/01/big-blue-unveils-bob-premium-pack-for-ibm-i/) — 좌석당 지원 비용, 엔터프라이즈 풀 코인
- [BobShell 자기 문서화 (DevOps.com)](https://devops.com/ibm-bob-takes-ai-coding-assistants-to-the-next-level/) — IBM 주장 인용
- [프롬프트 인젝션 보고 (The Register, 2026-01-07)](https://www.theregister.com/2026/01/07/ibm_bob_vulnerability/)
- [Bob-IDE가 VS Code 포크라는 점 (IT Jungle)](https://www.itjungle.com/2025/12/08/guru-a-first-look-at-bob-the-ibm-i-assistant-thats-closer-than-you-think/)

**경쟁 도구 — 1차 제품 문서**

Bob과 같은 등급의 근거를 맞추기 위해 개요 페이지가 아니라 주장별 개별 문서를 확인했다.

- Claude Code: [개요·실행 표면](https://code.claude.com/docs/en/overview) ·
  [Routines](https://code.claude.com/docs/en/routines) ·
  [Hooks](https://code.claude.com/docs/en/hooks) ·
  [Skills](https://code.claude.com/docs/en/skills) ·
  [Sandboxing](https://code.claude.com/docs/en/sandboxing) ·
  [IAM](https://code.claude.com/docs/en/iam) ·
  [Agent SDK](https://code.claude.com/docs/en/agent-sdk/overview) ·
  [GitHub Actions](https://code.claude.com/docs/en/github-actions) ·
  [웹 세션](https://code.claude.com/docs/en/claude-code-on-the-web) ·
  [엔터프라이즈 배포](https://code.claude.com/docs/en/third-party-integrations) ·
  [Analytics API](https://platform.claude.com/docs/en/manage-claude/analytics-api)
- Codex: [문서](https://learn.chatgpt.com/docs) —
  이전 판이 인용한 `developers.openai.com/codex/`는 308로 이 주소로 넘어간다 ·
  [업데이트 발표](https://openai.com/index/introducing-upgrades-to-codex/)
- Cursor: [문서](https://cursor.com/docs) · [CLI](https://cursor.com/cli) ·
  [Automations](https://cursor.com/docs/cloud-agent/automations) ·
  [Slack 백그라운드 에이전트](https://cursor.com/changelog/1-1) ·
  [요금제](https://www.cursor.com/pricing)
- [Claude Cowork 웹·모바일 확장 (TechCrunch, 2026-07-07)](https://techcrunch.com/2026/07/07/the-coding-agent-wars-are-spilling-into-the-rest-of-the-office-claude-cowork/) — 보도 등급
